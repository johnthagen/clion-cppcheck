<idea-plugin>
  <id>com.github.johnthagen.cppcheck</id>
  <name>cppcheck</name>
  <version>1.0.9</version>
  <vendor email="johnthagen@gmail.com" url="http://github.com/johnthagen">johnthagen</vendor>

  <description><![CDATA[
      Cppcheck plugin for CLion.<br/><br/>

      <b>Features:</b><br/>
      <ul>
        <li>Runs <tt>cppcheck</tt> on the fly while you write code</li>
        <li>Highlights lines and displays <tt>cppcheck</tt> error messages</li>
        <li>Supports passing options to <tt>cppcheck</tt></li>
      </ul>
      <br/>

      <b>Usage:</b><br/>
      <ul>
        <li>
          Install the <a href="http://cppcheck.sourceforge.net/">cppcheck tool</a> using directions on its homepage.
          This plugin does <b>not</b> bundle the <tt>cppcheck</tt> tool itself, which must be installed separately.
        </li>
        <li>Install this cppcheck plugin into CLion</li>
        <li>Configure the plugin with the <b>absolute</b> path the to <tt>cppcheck</tt> executable into the
        'cppcheck path' configuration field</li>
        <ul>
          <li>Windows
            <ul>
              <li>File | Settings | cppcheck configuration</li>
              <li>Usually the path is <tt>C:\Program Files (x86)\Cppcheck\cppcheck.exe</tt></li>
            </ul>
          </li>
          <li>macOS
             <ul>
               <li>CLion | Preferences | cppcheck configuration</li>
               <li>In a terminal run <tt>which cppcheck</tt> to find the path to cppcheck.
               If you installed it with <a href="https://brew.sh/">Homebrew</a>, the path will be
               <tt>/usr/local/bin/cppcheck</tt></li>
             </ul>
          </li>
          <li>Linux
            <ul>
              <li>File | Settings | cppcheck configuration</li>
              <li>In a terminal run <tt>which cppcheck</tt> to find the path to cppcheck.
              If you installed it with your system's package manager, it is probably located at
              <tt>/usr/bin/cppcheck</tt></li>
            </ul>
          </li>
        </u1>
      </ul>
      <br/>

      <b>Known issues:</b><br/>
        <tt>cppcheck</tt> is not designed to be run on header files (<tt>.h</tt>) directly, as must be done for this
        plugin, and as a result may have false positives.<br/>
        When run on header files directly, <tt>cppcheck</tt> defaults to C as the language, which will
        generate false positives for C++ projects. C++ projects should leave <tt>--language=c++</tt> appended to the
        cppcheck options.<br/>
    ]]></description>

  <change-notes><![CDATA[
      1.0.9 Improve guide instructions<br/>
    ]]>
  </change-notes>

  <!-- please see http://www.jetbrains.org/intellij/sdk/docs/basics/getting_started/build_number_ranges.html for description -->
  <idea-version since-build="143.0"/>

  <!-- please see http://www.jetbrains.org/intellij/sdk/docs/basics/getting_started/plugin_compatibility.html
       on how to target different products -->
  <!-- enable plugin in all products -->
  <depends>com.intellij.modules.lang</depends>

  <extensions defaultExtensionNs="com.intellij">
    <localInspection implementationClass="com.github.johnthagen.cppcheck.CppcheckInspection"
                     displayName="cppcheck inspections"
                     groupName="cppcheck"
                     id="CppcheckInspection"
                     level="WARNING"
                     enabledByDefault="true"/>
    <applicationConfigurable instance="com.github.johnthagen.cppcheck.Configuration"
                             displayName="cppcheck configuration"/>
  </extensions>

  <actions>
    <!-- Add your actions here -->
  </actions>

</idea-plugin>